# Local A6000 driver 535 cannot run the organizer's CUDA 13 wheel; use the
# same CUDA 12-compatible torch release as the organizer BNB baselines.
torch==2.7.0

transformers>=4.51.1,<5
huggingface-hub>=0.34.0,<1.0
tokenizers>=0.22.0,<=0.23.0
Pillow>=10.0.1,<=15.0

accelerate==1.6.0
tqdm>=4.67.0
